<!DOCTYPE html>
<html>
  <head>
    <title>Foodlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <%= render "shared/header" %>
  <% if notice.present? %>
  <p id="notification is-link is-light"><% notice %></p>
  <% end %>
  <% if alert.present? %>
  <p id="notification is-danger is-light"><%= alert %></p>
  <% end %>
  <div class="container">
    <%= yield %>
    <div>
  </body>
</html>
